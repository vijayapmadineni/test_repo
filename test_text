import datetime
import pytz

def is_alternate_wednesday(start_date, today):
    """Check if today is an alternate Wednesday starting from the start date."""
    delta_days = (today - start_date).days
    return delta_days % 14 == 0

def test_alternate_wednesdays(start_date, num_weeks=52):
    """Test the alternate Wednesday logic for a range of weeks."""
    eastern = pytz.timezone("US/Eastern")
    current_date = start_date

    # Iterate through dates to validate alternate Wednesdays
    print(f"Starting from {start_date} (Eastern Time):")
    for week in range(num_weeks):
        # Advance to the next Wednesday
        current_date += datetime.timedelta(days=7)
        current_date_et = datetime.datetime.combine(current_date, datetime.datetime.min.time(), pytz.utc).astimezone(eastern).date()

        # Check if it's an alternate Wednesday
        if is_alternate_wednesday(start_date, current_date_et):
            print(f"  {current_date_et} is an alternate Wednesday")
        else:
            print(f"  {current_date_et} is NOT an alternate Wednesday")

# Define the start date (2024-12-11)
start_date = datetime.date(2024, 12, 11)

# Run the test for the next 52 weeks
test_alternate_wednesdays(start_date)


Starting from 2024-12-11 (Eastern Time):
  2024-12-17 is NOT an alternate Wednesday
  2024-12-24 is NOT an alternate Wednesday
  2024-12-31 is NOT an alternate Wednesday
  2025-01-07 is NOT an alternate Wednesday
  2025-01-14 is NOT an alternate Wednesday
  2025-01-21 is NOT an alternate Wednesday
  2025-01-28 is NOT an alternate Wednesday
  2025-02-04 is NOT an alternate Wednesday
  2025-02-11 is NOT an alternate Wednesday
  2025-02-18 is NOT an alternate Wednesday
  2025-02-25 is NOT an alternate Wednesday
  2025-03-04 is NOT an alternate Wednesday
  2025-03-11 is NOT an alternate Wednesday
  2025-03-18 is NOT an alternate Wednesday
  2025-03-25 is NOT an alternate Wednesday
  2025-04-01 is NOT an alternate Wednesday
  2025-04-08 is NOT an alternate Wednesday
  2025-04-15 is NOT an alternate Wednesday
  2025-04-22 is NOT an alternate Wednesday
